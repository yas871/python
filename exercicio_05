# Leitura dos números
numero1 = float(input("Digite o primeiro número: "))
numero2 = float(input("Digite o segundo número: "))

# Realizando as operações
soma = numero1 + numero2
subtracao = numero1 - numero2
multiplicacao = numero1 * numero2

# Verificando se a divisão é válida (não pode dividir por zero)
if numero2 != 0:
    divisao = numero1 / numero2
    print(f"A divisão de {numero1} por {numero2} é {divisao:.2f}.")
else:
    print("Não é possível dividir por zero.")

# Exibindo os resultados
print(f"A soma de {numero1} e {numero2} é {soma}.")
print(f"A subtração de {numero1} e {numero2} é {subtracao}.")
print(f"A multiplicação de {numero1} e {numero2} é {multiplicacao}.")
